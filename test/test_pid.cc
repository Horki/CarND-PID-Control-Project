#include <gtest/gtest.h>

#include "PID.h"

class PIDTest : public ::testing::Test {
  PID pid;
};

TEST(PidTest, test_pid) {
  ASSERT_EQ(true, true);
  ASSERT_TRUE(true);
  ASSERT_FALSE(false);
  // todo: add test
}
